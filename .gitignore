# don't rewrite the deployed conf file
demo.conf

# ignore compiled Python code
*.pyc

# ignore the linting flag
lint.flag

# ignore the temporary data files and thumbnails (locally generated)
*/data/tmp/*
*/data/input/*.__*__.png
